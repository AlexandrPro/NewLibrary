@model Library.ViewModels.Magazine.IndexMagazineViewModel

@{
    ViewBag.Title = "Index";
}

<h2>Magazines</h2>

@(Html.Kendo().Grid<Library.ViewModels.Magazine.MagazineViewModel>()
        .Name("grid")
        .Columns(columns =>
        {
            columns.Bound(c => c.Name);
            columns.Bound(c => c.Number);
            columns.Bound(c => c.YearOfPublishing);
        })
        .HtmlAttributes(new { style = "height: 550px;" })
        .Scrollable()
        .Groupable()
        .Sortable()
        .Pageable(pageable => pageable
            .Refresh(true)
            .PageSizes(true)
            .ButtonCount(5))
        .DataSource(dataSource => dataSource
            .Ajax()
            .Read(read => read.Action("Magazines_Read", "Magazine"))
            .PageSize(20)
        )
)